<?xml version="1.0" encoding="utf-8"?>
<!--
  Denies cleartext HTTP everywhere except the local dev backend addresses.

  IMPORTANT: this does NOT constrain the app's own API calls. Flutter's
  dart:io HttpClient (which package:http sits on) opens its own sockets and
  never consults Android's NetworkSecurityPolicy, so neither this file nor the
  targetSdk>=28 cleartext default applies to it — verified on a release build
  that reached http://10.0.2.2:3000 with no config present at all. The guard
  that actually enforces HTTPS for API traffic lives in lib/core/api_client.dart.

  This file is here for the Java/Kotlin-side network stacks (HttpURLConnection,
  OkHttp, WebView, Cronet) used by the platform and by any plugin added later,
  so such a dependency can't quietly start talking plaintext to a real host.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <!-- The Android emulator's alias for the host machine's loopback. -->
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
